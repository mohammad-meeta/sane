<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Welcome page
    link(rel="stylesheet" href="styles/core/app.css")

  body.login-container
    h1 Hello world
      h2 #{ message }
      a(href=Helper.route("auth.login")) Login
      script(defer src=Helper.mix("scripts/app.js"))
